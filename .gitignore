node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Local data — the SQLite database and anything derived from it
data/
!data/.gitkeep

# Scraper fixtures are checked in on purpose (see .gitignore exception below),
# but downloaded media never is.
media/
*.mp4
*.mp3
*.wav

.env
.env.local
.env*.local

.DS_Store
Thumbs.db

# Probe output — contains real account data, follower usernames, and token metadata.
graph-probe.json
apify-followers-probe.json
migration-snapshot*.json
