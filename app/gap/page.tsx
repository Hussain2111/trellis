import { MilestonePlaceholder } from '@/components/milestone-placeholder';

export default function Page(): React.JSX.Element {
  return (
    <MilestonePlaceholder
      title="Gap"
      milestone="M5"
      description="Five winning patterns and the one gap worth closing, each with its numbers and the posts behind them."
    />
  );
}
